{
	"name": "dev",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"sass": "sass --no-source-map --update --watch --style=compressed ./scss:../assets/css",
		"gulp": "gulp",
		"dev": "concurrently \" npm run sass\" \" npm run gulp\" "
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"concurrently": "^9.1.0",
		"gulp": "^5.0.0",
		"gulp-rename": "^2.0.0",
		"gulp-uglify": "^3.0.2",
		"sass": "^1.82.0"
	}
}
